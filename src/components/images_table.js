let images_items_table = (img) => {
  return `<img id="img-${img.name}-table" class="plant-img hidden transform scale-0 rotate-0" src="${img.images.png}" alt="" />`;
};
export default images_items_table;
