const Pagination = (item) => {
  return ` <div id="${item.name}"
                  class="w-[10px] cursor-pointer bg-[rgba(151,151,151,.5)] h-[10px] rounded-full"
                ></div>
              `;
};
export default Pagination;
